# Core dependencies
fastapi==0.116.1
uvicorn==0.35.0
requests==2.31.0
python-dotenv==1.1.1
feedparser==6.0.12
pydantic-settings==2.10.1

# LangChain and related libraries (specific versions to avoid unnecessary extras)
langchain==0.3.27
chromadb==1.0.21

# Optional, only if explicitly needed and verified
langchain-groq==0.3.8
groq==0.31.1
